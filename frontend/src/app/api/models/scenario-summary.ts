/* tslint:disable */
/* eslint-disable */

/**
 * One entry of the library: `GET /scenarios`.
 */
export interface ScenarioSummary {
  description: string;

  /**
   * The scenario id, matching the ScenarioId enum's values.
   */
  id: string;
  name: string;
}
